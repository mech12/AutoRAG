# =============================================================================
# AutoRAG 환경변수 설정 예시
# =============================================================================
# 이 파일을 복사하여 .env 파일을 생성하세요:
#   cp .env.example .env
#
# .env 파일은 git에 커밋되지 않습니다 (보안상의 이유)
# =============================================================================

# =============================================================================
# 커스텀 LLM 서버 (vLLM 호환)
# =============================================================================
# vLLM 또는 OpenAI 호환 LLM 서버 URL
# 주의: /v1 경로를 포함해야 합니다
CUSTOM_LLM_API_BASE=https://your-llm-server.com/v1

# LLM 모델 이름 (vLLM에서 호스팅하는 모델)
CUSTOM_LLM_MODEL=openai/your-model-name

# API 키 (자체 서버는 빈 문자열 사용)
CUSTOM_LLM_API_KEY=

# =============================================================================
# 커스텀 임베딩 서버 (Infinity/OpenAI 호환)
# =============================================================================
# 임베딩 서버 URL
# 주의: Infinity 서버는 /v1 경로를 포함하지 않습니다
CUSTOM_EMBEDDING_API_BASE=http://your-embedding-server:8081

# 임베딩 모델 이름
CUSTOM_EMBEDDING_MODEL=BAAI/bge-m3

# =============================================================================
# Milvus Vector Database (선택사항)
# =============================================================================
# Milvus 서버 연결 설정
MILVUS_HOST=10.10.20.94
MILVUS_PORT=19530
MILVUS_USER=minioadmin
MILVUS_PASSWORD=minioadmin

# 컬렉션 이름 프리픽스 (테스트케이스별 자동 생성)
# 예: MILVUS_COLLECTION_NAME_PREFIX=kb -> kb_autorag_인사규정
MILVUS_COLLECTION_NAME_PREFIX=kb

# 또는 고정 컬렉션 이름 사용 (PREFIX보다 우선)
# MILVUS_COLLECTION_NAME=my_fixed_collection
